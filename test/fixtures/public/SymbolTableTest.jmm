import static io.read() int;
import static io.println() void;
import static io.println(int) void;

import static Math.sort() int;

class Life extends Dead{
    

    public static void main(String[] a) {
        Life d;
        d = new Life();
        d.inbounds(4, 4);

        
    }

    public Life inbounds(int row, int column) {
        Life here;
        boolean in;
        int f;
        f = 1;
        in = true;
        here = new Life();
        if (row < 0){
            if(f < 2){
                in = false;
            }else{
                in = false;
            }
        }
        else {
            in = true;
        }


        while(row < 0){

        }
            
        return here.inbounds(row, column);
    }


/*
    public boolean quicksort(int[] L) {
        int xMax;
        boolean lazy;
        Life p;

        p = this;

        if ( 5 < 4 ) {
            //this.beLazy(L);
            lazy = true;
        }
        else {
            lazy = false;
        }
        
        if ( lazy ) {
            lazy = !lazy;
        }
        else {
            lazy = this.quicksort(L);
        }
        
        this.quicksort(L);
        return lazy;
    }

    public boolean quicksort(int[] L, String teste) {
        boolean lazy;
        if ( MathUtils.random(0, 5) < 4 ) {
            this.beLazy(L);
            lazy = true;
        }
        else {
            lazy = false;
        }
        
        if ( lazy ) {
            lazy = !lazy;
        }
        else {
            lazy = int.quicksort(L, 0, L.length - 1);
        }
        
        return lazy;
    }*/
}
