import static io.read() int;
import static io.println() void;
import static io.println(int) void;
class Life extends Dead{

    int UNDERPOP_LIM;
    int OVERPOP_LIM;
    int REPRODUCE_NUM;

    int LOOPS_PER_MS;

    int xMax;
    int yMax;
    int[] field;

    public static void main(String[] a) {
		int[] teste;
        ola teste2;
        Life teste3;
        int[] b;
        int a;
        boolean c;
        
        
        //teste = new int[b][0];
        c = (b[0]/4 * a * a + 3 *2/b[4] - (4*5)) < a && teste3.quicksort(teste2) < 3;
        b[teste3.quicksort(teste2)] = a;
        //a = teste3.quicksort(b, teste2);
        //a = b.length;

    }

    public int quicksort(ola z) {
        int xMax;
        boolean lazy;
        Life p;
        p = this;
        if ( MathUtils.random(0, 5) < 4 ) {
            this.beLazy(L);
            lazy = true;
        }
        else {
            lazy = false;
        }
        
        if ( lazy ) {
            lazy = !lazy;
        }
        else {
            lazy = this.quicksort(L, 0, L.length - 1);
        }
        
        return lazy;
    }
/*
    public boolean quicksort(int[] L, String teste) {
        boolean lazy;
        if ( MathUtils.random(0, 5) < 4 ) {
            this.beLazy(L);
            lazy = true;
        }
        else {
            lazy = false;
        }
        
        if ( lazy ) {
            lazy = !lazy;
        }
        else {
            lazy = this.quicksort(L, 0, L.length - 1);
        }
        
        return lazy;
    }*/
}
