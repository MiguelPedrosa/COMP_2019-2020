import static io.read() int;
import static io.println() void;
import static io.println(int) void;
class Life extends Dead{

    int UNDERPOP_LIM;
    int OVERPOP_LIM;
    int REPRODUCE_NUM;

    int LOOPS_PER_MS;

    int xMax;
    int yMax;
    int[] field;

    public static void main(String[] a) {
		Life l;
        Life l2;
		int unused;
        int xMax;
        boolean teste;
        Dead teste3;

        teste[0] = 4;

		unused = 4;
        teste = true;
        teste = false;
        l = this;
    
        l2 = teste3;

		l.init();

        while (true) {
            l.printField();
            l.update();
            unused = io.read();
        }

    }

    public boolean quicksort(int[] L) {
        int xMax;
        boolean lazy;
        if ( MathUtils.random(0, 5) < 4 ) {
            this.beLazy(L);
            lazy = true;
        }
        else {
            lazy = false;
        }
        
        if ( lazy ) {
            lazy = !lazy;
        }
        else {
            lazy = this.quicksort(L, 0, L.length - 1);
        }
        
        return lazy;
    }

    public boolean quicksort2(int[] L, String teste) {
        boolean lazy;
        if ( MathUtils.random(0, 5) < 4 ) {
            this.beLazy(L);
            lazy = true;
        }
        else {
            lazy = false;
        }
        
        if ( lazy ) {
            lazy = !lazy;
        }
        else {
            lazy = this.quicksort(L, 0, L.length - 1);
        }
        
        return lazy;
    }
}
